% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ss.R
\name{plot_ss}
\alias{plot_ss}
\title{Visualise residuals and sum of squares for a fitted line}
\usage{
plot_ss(x, y, showSquares = FALSE, leastSquares = FALSE)
}
\arguments{
\item{x}{Numeric vector of x-values.}

\item{y}{Numeric vector of y-values (same length as \code{x}).}

\item{showSquares}{Logical; if \code{TRUE}, draw squares illustrating squared residuals.}

\item{leastSquares}{Logical; if \code{TRUE}, fit the least-squares line using \code{\link[stats:lm]{stats::lm()}}.}
}
\value{
Invisibly returns a list containing:
\itemize{
\item \code{model}: the fitted \code{lm} object
\item \code{ss}: the residual sum of squares
}
The function is mainly used for its plot and printed output.
}
\description{
Produces a scatter plot of \code{y} versus \code{x} and fits a line either by:
(1) letting the user click two points on the plot, or
(2) fitting the least-squares regression line.
}
\details{
The function then computes residuals and prints the sum of squared residuals.
Optionally, it draws "squares" whose side lengths correspond to residual sizes
as a visual demonstration of least squares.
}
\examples{
set.seed(1)
x <- 1:10
y <- 1 + 0.5 * x + rnorm(10, sd = 0.5)
plot_ss(x, y, leastSquares = TRUE)

}
